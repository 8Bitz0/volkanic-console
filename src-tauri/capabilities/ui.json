{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ui",
  "description": "Capabilities for UI",
  "windows": ["main"],
  "permissions": [
    "dialog:default",
    "dialog:allow-message"
  ]
}
